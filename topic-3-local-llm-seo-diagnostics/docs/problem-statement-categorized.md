<!--
Traceability:
  Generated by: prompts/master-prompt.md → categorize-topic-requirement, {#}=3
  Source file: topic-3-local-llm-seo-diagnostics/docs/problem-statement.md
  Category schema: prompts/prompt-template.md (Role, Objective, Constraints,
    Reference, Deliverables, Evaluation Criteria)
  Method: source text split into self-contained, ID-tagged bullets (B1..B21),
    each mapped to its best-fit template category; compound sentences split
    where they straddled two categories.
-->

# Topic 3 — Categorized Requirements: Local LLM SEO Diagnostics

## Run notes (step 4 feasibility statement)
- Categorization was fully feasible; no missing or ambiguous source content.
- Unlike topic 2 (no persona), this topic has an explicit Role statement (B5):
  the model must be framed as a "Google official SEO quality reviewer."
- This topic has a dedicated "測驗流程 (Workflow)" section not present in the
  same form in topics 1–2. Its steps were categorized individually rather than
  bulk-tagged as one block: the data-split rule (12 rows) → Constraints, the
  dataset itself → Reference, batch inference / post-processing → Objective
  (they describe required system behavior, not rules governing how it's done).
- B16/B17 were split: the audit-report deliverable itself vs. the strict
  train/test-separation rule governing how it must be produced.
- **Exception, explicitly requested rather than silently substituted**: B3
  ("Deploy via Kaggle Kernels") and B15 ("A Kaggle Notebook...") both name
  Kaggle specifically. Per the project owner's explicit direction, this
  deploys on **Google Colab with a T4 GPU** instead — comparable free-tier
  GPU capability (both offer a T4-class card suitable for an 8B model with
  4-bit quantization + QLoRA), chosen for consistency with topics 1-2's
  already-established Colab workflow rather than adding a third platform's
  account setup. B3/B15 are not satisfied literally; this is a deliberate,
  named exemption, not an oversight.
- Model: **Llama-3-8B-Instruct** (one of B3's two named options). Gated on
  Hugging Face — needs the project owner's own HF account to accept Meta's
  license on the model page, plus an HF access token set as a Colab secret,
  before the notebook can actually download the weights.

## Role
| ID | Statement |
|----|-----------|
| B5 | The Complex System Prompt must define the model's persona as a "Google official SEO quality reviewer" (Google 官方 SEO 品質審核員). |

## Objective
| ID | Statement |
|----|-----------|
| B2 | The system must have the sensitivity/professionalism of a senior industry SEO editor: able to identify semantic defects, keyword stuffing, and YMYL compliance issues in articles. |
| B4 | Design a Complex System Prompt for the deployed model. |
| B7 | EEAT diagnostic capability: detect whether an article contains "false experience" claims (e.g. in a second-mortgage article, judge whether the advice aligns with Taiwanese legal common sense). |
| B8 | Consistency test: run the same article through repeated audits three times, testing output consistency across different Temperature settings. |
| B10 | The system must identify and correct deliberately planted false information, demonstrating independent knowledge-base verification rather than blindly following the source text. |
| B13 | Batch Inference: use the model to run diagnostics on the data in batch. |
| B14 | Post-processing: parse the JSON output into an interactive HTML Report or a Pandas DataFrame for administrator review. |

## Constraints
| ID | Statement |
|----|-----------|
| B3 | Deploy via Kaggle Kernels using Llama-3-8B-Instruct, Gemma-2-9B-IT, or an equivalent-tier model. |
| B6 | Model output must be in JSON format, containing: Score, Reasons (扣分原因), and Actionable Advice (具體建議). |
| B12 | 12 randomly selected rows from the provided CSV dataset must be used for fine-tuning/training. |
| B17 | Training data and audit/test data must be strictly separated with zero overlap. |

## Reference
| ID | Statement |
|----|-----------|
| B1 | A "SEO content audit robot" is needed that runs 24/7 and does not leak data externally. |
| B9 | The test data contains a deliberately planted false SEO theory (e.g. claiming Meta Keywords tags are still a key ranking factor in 2026). |
| B11 | The CSV sample dataset (SEO 診斷測試數據樣本) provided for this task is the source of article snippets. |

## Deliverables
| ID | Statement |
|----|-----------|
| B15 | A Kaggle Notebook demonstrating the complete code flow and documentation, from deployment through report generation. |
| B16 | An Audit Report example built from the 8 rows not used in fine-tuning/training, explaining why the model's judgments were accurate or where they showed bias. |
| B18 | A Reflection explaining the chosen local LLM's strengths/weaknesses when handling Chinese-language SEO context, and how prompt design was used to improve its output quality. |

## Evaluation Criteria
| ID | Statement |
|----|-----------|
| B19 | Diagnostic Quality & Reasoning Depth: EEAT discernment (e.g. pointing out missing Experience), YMYL sensitivity (e.g. identifying legal risk or overpromising language such as "guaranteed profit"), and whether advice has practical value. |
| B20 | Error Detection & Knowledge Independence: identifying the deliberately planted "incorrect SEO theory" in the dataset. |
| B21 | Output Standard & Structural Integrity: whether output is 100% compliant with the predefined JSON format. |
