<!--
Traceability:
  Generated by: prompts/master-prompt.md → categorize-topic-requirement, {#}=4
  Source file: topic-4-multi-agent-conflict-resolution/docs/problem-statement.md
  Category schema: prompts/prompt-template.md (Role, Objective, Constraints,
    Reference, Deliverables, Evaluation Criteria)
  Method: source text split into self-contained, ID-tagged bullets (B1..B19),
    each mapped to its best-fit template category; compound sentences split
    where they straddled two categories.
-->

# Topic 4 — Categorized Requirements: Multi-Agent Conflict Resolution

## Run notes (step 4 feasibility statement)
- Categorization was fully feasible; no missing or ambiguous source content.
- Unlike topics 2 (no persona) and 3 (one persona), this topic defines **two**
  Role statements — one per agent (B4, B5) — since Agent A and Agent B have
  distinct, deliberately opposed objectives that drive the whole exercise.
- Split the "Source Code" deliverable (B14) from the max-iterations rule it
  mentions (B15): the source code artifact is a Deliverable, but "must cap
  iterations to prevent infinite loops" is a Constraint governing runtime
  behavior, following the same Deliverable-vs-Constraint split used for
  topic 3's B16/B17.
- B9/B10 (the forced-conflict scenario and the specific prohibited-phrase
  setup) are treated as Reference: they describe a fixed test fixture the
  system must be run against, not an action to perform.

**Backend decision, asked rather than silently picked**: `docs/architecture.md`
had a stale, pre-topic-1 note — "Topic 4 uses LangGraph with a Llama-based
model (decided; exact model/runtime TBD)" — that conflicted with what's
actually been proven out since (Azure OpenAI in topics 1-2; a local
fine-tuned Llama-3-8B in topic 3, GPU/Colab-dependent). Asked the project
owner directly; **Azure OpenAI for both Agent A and Agent B**, consistent
with topics 1-2, no GPU dependency for this topic. `docs/architecture.md`'s
note is superseded by this decision.

**Real finding, checked before building anything**: `data/Manual.txt`
(the "company's internal manual" B5 references) turns out to be **entirely
mortgage/interest-rate-specific** — its 3 rules require an
"individual-credit-conditions-apply" disclaimer on interest-rate claims,
require disclosing the bank-vs-private-lender legal distinction, and ban
"guaranteed approval"/"lowest rate nationwide" language. **It contains no
casino-related rule at all** — not "穩賺不賠", not "保證出金", not anything
resembling B10's forced-conflict phrases. So B10's specific forbidden-term
list is not derivable from Manual.txt; it's used here as its own explicit
fixture rule (as B10 itself already states — "the Compliance Auditor's
rules strictly forbid such terms"), not something invented independently or
force-fit from a document that doesn't cover this content category. Manual.txt
is still loaded as Agent B's general-purpose compliance reference (so a
mortgage-content run would be governed by its real rules too), just not the
source of this specific test scenario's conflict.

**Design decision, flagged**: Manual.txt is loaded as **static text directly
in Agent B's prompt**, not retrieved via topic 2's ChromaDB/RAG pipeline.
Topic 2's own postmortem already named why: at 3 lines total, real
retrieval has no discrimination to perform (`top_k` would always return
everything) — reusing that machinery here would add complexity without
adding any real capability, not a shortcut around a requirement that
actually calls for retrieval.

## Role
| ID | Statement |
|----|-----------|
| B4 | Agent A — SEO Content Planner: goal is to maximize search click-through rate; prefers eye-catching, highly competitive keywords and titles. |
| B5 | Agent B — YMYL Compliance Auditor: goal is to ensure content is 100% compliant with legal regulations and the company's internal manual (see topic 2 data); zero tolerance for exaggerated, misleading, or high-risk wording. |

## Objective
| ID | Statement |
|----|-----------|
| B2 | Build an automated workflow simulating two AI Agents with different responsibilities collaborating. |
| B3 | The system must demonstrate reaching final consensus through Recursive Refinement when the agents disagree, not just produce a result. |
| B6 | Build a Stateful graph architecture. |
| B11 | Demonstrate, through inter-agent dialogue, how the system automatically converges on a compromise that is both click-attracting and regulation-compliant (e.g. honestly disclosing win rates, emphasizing security/audit compliance). |

## Constraints
| ID | Statement |
|----|-----------|
| B7 | The flow must include: Start -> Planning -> Audit -> Decision Node (conditional determination). |
| B8 | Conditional Edge: if Audit passes, output the final result; if Audit fails, return specific revision suggestions and route back to the Planning node to regenerate. |
| B15 | The system must impose a limit on Maximum Iterations to prevent infinite loops. |

## Reference
| ID | Statement |
|----|-----------|
| B1 | In real-world SEO business, content teams often face a "traffic" vs. "compliance" dilemma. |
| B9 | Test scenario: the user requests content planning for "娛樂城推薦" (casino recommendation). |
| B10 | Conflict fixture: the SEO Planner is configured to require high-CTR keywords like "穩賺不賠" (guaranteed profit) or "保證出金" (guaranteed payout); the Compliance Auditor's rules strictly forbid such terms. |

## Deliverables
| ID | Statement |
|----|-----------|
| B12 | Graph Visualization: a graphical rendering of the LangGraph workflow (e.g. a Mermaid flowchart), clearly labeling nodes and edge logic. |
| B13 | State Logs: a set of conversation records showing the complete "negotiate" and "revert for revision" process between Agent A and Agent B around the conflict point. |
| B14 | Source Code, including the definition of the State Schema. |
| B16 | Final Output: the final article outline produced after multiple rounds of negotiation, plus a summary highlighting the key points of that process. |

## Evaluation Criteria
| ID | Statement |
|----|-----------|
| B17 | Logic & Flow: can the system precisely determine when to end the loop? Does the State object fully carry prior conversation context so the Planner knows why it was rejected? |
| B18 | Reasoning Quality: are Agent B's revision suggestions constructive enough to guide Agent A toward correct changes? Does the final output genuinely balance SEO and compliance rather than one-sidedly conceding to either side? |
| B19 | Technical Maturity: are asynchronous calls handled correctly? Is there basic fault-tolerance design for LLM latency and API errors? |
